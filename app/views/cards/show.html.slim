div class='p-4 border-black border-4 rounded'
   p #{t('cards.original_text')}: #{@card.original_text}
   p #{t('cards.translated_text')}: #{@card.translated_text}
   p #{t('cards.review_date') }: #{@card.review_date.strftime('%d/%m/%Y')}
   